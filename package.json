{
  "name": "rust-wasm-start",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run build && npm run start",
    "build": "pnpm -r run build",
    "start": "pnpm i && cd app && npm run serve",
    "init": "pnpm i && npm run build && pnpm i"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.6.2",
    "@types/node": "^16.11.7",
    "tslib": "^2.3.1",
    "typescript": "^4.6.3",
    "wasm-pack": "^0.10.2"
  }
}
